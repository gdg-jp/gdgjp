ImageIdPath:
  name: id
  in: path
  required: true
  schema:
    type: string
    pattern: '^[0-9A-Za-z]{8}$'
    minLength: 8
    maxLength: 8
FolderIdPath:
  name: id
  in: path
  required: true
  schema:
    type: integer
