Error:
  type: object
  required: [error]
  properties:
    error:
      type: string
CliArchiveResult:
  type: object
  required: [id, archived]
  properties:
    id:
      type: integer
    archived:
      type: boolean
      enum: [true]
