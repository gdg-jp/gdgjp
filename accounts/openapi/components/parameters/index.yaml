ChapterQuery:
  name: q
  in: query
  required: false
  schema:
    type: string
    maxLength: 200
ClientId:
  name: client_id
  in: query
  required: true
  schema:
    type: string
    minLength: 1
